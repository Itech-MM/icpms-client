<Window x:Class="icpms_client.Utils.UI.Dialog.ExportDialog.ExportDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:fa="http://schemas.fontawesome.io/icons/"
        mc:Ignorable="d"
        Title="ExportDialog"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        ShowInTaskbar="False"
        Topmost="True"
        WindowStartupLocation="CenterOwner">
    
    <Window.Resources>
        <!-- Button Container Style -->
        <Style x:Key="ButtonContainerStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Padding" Value="1"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="Background" Value="#888888"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Tag, RelativeSource={RelativeSource Self}}" Value="excel">
                    <Setter Property="Background" Value="#28C76F"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Tag, RelativeSource={RelativeSource Self}}" Value="pdf">
                    <Setter Property="Background" Value="#EA5455"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        
        <!-- Button Content Style -->
        <Style x:Key="ButtonContentStyle" TargetType="StackPanel">
            <Setter Property="Orientation" Value="Horizontal"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        
        <!-- Button Style -->
        <Style x:Key="ExportButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="{Binding CornerRadius, 
                                            RelativeSource={RelativeSource AncestorType=Border}}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"
                                              Content="{TemplateBinding Content}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#20000000"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#40000000"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <!-- Icon Style -->
        <Style TargetType="fa:ImageAwesome">
            <Setter Property="Width" Value="16"/>
            <Setter Property="Height" Value="16"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Grid Background="Transparent" VerticalAlignment="Center" HorizontalAlignment="Center">
        <Border MaxWidth="450" BorderBrush="{DynamicResource PrimaryBorderColor}" 
                BorderThickness="1" CornerRadius="8" Margin="10"
                Background="{DynamicResource PrimaryBackgroundColor}">
            <Border.Effect>
                <DropShadowEffect Color="Black" BlurRadius="15" ShadowDepth="5" Opacity="0.2" />
            </Border.Effect>
            
            <Grid MinWidth="400" Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- Header -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,10,0,15">
                    <fa:ImageAwesome Icon="Download" Foreground="#007ACC" Width="24" Height="24" Margin="0,0,10,0"/>
                    <TextBlock Text="Export Data" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Subtext -->
                <TextBlock Grid.Row="1" Text="Select the format for your export:"
                           FontSize="14" Foreground="#A0A0A0" Margin="0,0,0,20"/>

                <!-- Buttons -->
                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                    <!-- Cancel Button -->
                    <Border Style="{StaticResource ButtonContainerStyle}">
                        <Button Style="{StaticResource ExportButtonStyle}" Click="Cancel_Click">
                            <StackPanel Style="{StaticResource ButtonContentStyle}">
                                <TextBlock Text="Cancel"/>
                            </StackPanel>
                        </Button>
                    </Border>
                    
                    <!-- Excel Button -->
                    <Border Style="{StaticResource ButtonContainerStyle}" Tag="excel">
                        <Button Style="{StaticResource ExportButtonStyle}" Click="ExportExcel_Click">
                            <StackPanel Style="{StaticResource ButtonContentStyle}">
                                <fa:ImageAwesome Icon="FileExcelOutline"/>
                                <TextBlock VerticalAlignment="Center"  Text="Excel"/>
                            </StackPanel>
                        </Button>
                    </Border>
                    
                    <!-- PDF Button -->
                    <Border Style="{StaticResource ButtonContainerStyle}" Tag="pdf">
                        <Button Style="{StaticResource ExportButtonStyle}" Click="ExportPdf_Click">
                            <StackPanel Style="{StaticResource ButtonContentStyle}">
                                <fa:ImageAwesome Icon="FilePdfOutline"/>
                                <TextBlock VerticalAlignment="Center"  Text="PDF"/>
                            </StackPanel>
                        </Button>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>