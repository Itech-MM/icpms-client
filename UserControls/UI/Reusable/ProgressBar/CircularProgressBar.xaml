<UserControl x:Class="icpms_client.UserControls.UI.Reusable.ProgressBar.CircularProgressBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Viewbox Stretch="Uniform">
        <Grid Width="100" Height="100" x:Name="RootGrid">
            <!-- Background Circle -->
            <Ellipse x:Name="BackgroundCircle"
                     Stroke="{Binding Background, RelativeSource={RelativeSource AncestorType=UserControl}}"
                     StrokeThickness="5"/>
            
            <!-- Determinate Progress Arc -->
            <Path x:Name="ProgressPath"
                  Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=UserControl}}"
                  StrokeThickness="5"
                  StrokeStartLineCap="Round"
                  StrokeEndLineCap="Round"
                  RenderTransformOrigin="0.5,0.5">
                <Path.RenderTransform>
                    <RotateTransform Angle="-90"/>
                </Path.RenderTransform>
            </Path>
            
            <!-- Indeterminate Progress Arc -->
            <Path x:Name="IndeterminatePath"
                  Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=UserControl}}"
                  StrokeThickness="5"
                  StrokeStartLineCap="Round"
                  StrokeEndLineCap="Round"
                  Visibility="Collapsed">
                <Path.Data>
                    <PathGeometry>
                        <PathFigure StartPoint="50,10">
                            <ArcSegment Point="50,90" 
                                         Size="40,40" 
                                         SweepDirection="Clockwise"/>
                        </PathFigure>
                    </PathGeometry>
                </Path.Data>
                <Path.RenderTransform>
                    <RotateTransform x:Name="IndeterminateRotate" 
                                     CenterX="50" CenterY="50" Angle="0"/>
                </Path.RenderTransform>
            </Path>
            
            <TextBlock x:Name="ProgressText"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Center"
                       FontSize="16"
                       FontWeight="Bold"
                       Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=UserControl}}"
                       Visibility="Collapsed"/>
        </Grid>
    </Viewbox>
</UserControl>
